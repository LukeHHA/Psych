TODO: Make sure that all rendering classes follow the exact same interface for things like OnUpdate() ect
TODO: Investigate if application class instance pointer can become inline static per c++17

cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug -DBUILD_TESTING=OFF
